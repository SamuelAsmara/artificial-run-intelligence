send coaches to the coach screen, and show them their own edits

A coach with twenty athletes opened the app on the athlete dashboard: their
own empty training screen, dashes in every tile, and a "Connect intervals.icu
· Build my training plan" prompt. Nothing was broken. The first thing the
product said to a coach was that it had nothing for them.

"/" now decides the front door by role, and the two auth routes default to "/"
rather than to /dashboard — so the rule lives in one file instead of three.
Coaches who also run still reach their own training from the strip at the top
of every coach screen, which is what it is for.

Migration 0014 added origin, planned_distance_original and adjusted_reason,
and nothing has ever read them. A coach could shorten a long run, write down
why, come back a week later and find a number with no history —
indistinguishable from anything the generator produced. The session editor now
shows who changed it, from what, and the reason, and only when somebody
actually did.

rosterFlags listed an athlete once per rule they tripped, so the one who is
both detraining and missed a session appeared twice on the triage board: two
rows, one person, and it reads as two problems. Each athlete keeps their
loudest flag now. That drops information deliberately — the board's job is to
name who to open, not to enumerate everything wrong with them.

And a form of -0.4 printed as "-0" in a column where the sign carries the
meaning, which reads as a rendering fault.

586 tests, 3 of them new.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01SwrFRrEWTDaEjCwqEZogWx
