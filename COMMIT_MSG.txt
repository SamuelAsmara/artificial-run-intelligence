docs: final submission package — merged PDF, HTML presentation, diagrams and document fixes

Submission folder (docs/הגשה סופית):

- "0 - Runi - תיק הגשה מאוחד.pdf": one file for submission — a cover page
  (product mark, lecturer, student, course), a master table of contents
  with the page each document starts on, PDF bookmarks, and the six
  documents in reading order (61 pages).
- "7 - מצגת - Runi.html": the presentation is now a single self-contained
  HTML deck in the product's own design language — 15 screens that follow
  the twelve points the course asks a presentation to cover, animated
  architecture, live screenshots, a "key insights" screen and speaker
  notes (N). The .pptx is retired.
- Document 2 gains three diagrams (page load, write path, the two
  permission layers) in place of step tables; document 6 gains the
  break-point chart and the client/server/database scheme.
- Documents 1-6: duplicate sections trimmed, counts aligned with the
  code (44 RLS policies, 17 tables, 13 functions), cross-references
  written as "סעיף N" instead of the § sign (which Word renders
  incorrectly in Hebrew), rounds table in document 4 re-laid, TOC fields
  set to refresh on open.
