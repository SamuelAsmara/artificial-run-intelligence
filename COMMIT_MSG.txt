feat(coach): cycles under their distance; roster filters distance → cycle

Cycles are listed under a distance heading — two half-marathon groups
(Tel Aviv, Jerusalem) sit under "Half marathon" — and the Athletes screen
filters the same way: pick a distance, then a cycle within it; athletes
not yet in a cycle stay reachable through their goal-race group. Each
roster row names the cycle its athlete is in.

fix: putting an athlete in a cycle was refused silently — the roster
row's UPDATE policy is athlete-only (0013, by design). Membership now
goes through set_athlete_cycle / clear_cycle_members (migration 0021,
applied), which touch cycle_id only and report whether a row changed.
