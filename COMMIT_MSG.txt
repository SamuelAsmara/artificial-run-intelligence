email: branded password-reset template; athlete wording on the payment mockup

The reset email used Supabase's default text next to a branded
confirmation email. templates/reset-password.html is the same shell with
the reset copy; email/README.md says where it goes and notes that a new
reset request invalidates the previous link.

The payment-method mockup on the athlete's Billing tab said "a coach" and
quoted the coach's Basic price; it now speaks to the athlete, whose Basic
is free with no end date.
